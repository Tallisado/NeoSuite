require 'rspec/core/rake_task'

desc 'Default: run tests against the selenium driver on the local machine (components should be currently running)'
namespace :test do
	puts "RUNNING FAKE WEBROBOT RAKE"
end
commander: nightly

neosuite_internal_sanity_multiple: { 
  toolbox: webrobot, 
  failover: die,
  raketask: 'local:headless',
  pattern: "dryrun_webrobot.rb",
  url: 'http://www.google.ca',
  tries: 1
}
# neosuite_internal_sanity_single: { 
  # toolbox: webrobot, 
  # failover: die,
  # raketask: 'local:headless',
  # pattern: "dryrun_screenshot_webrobot.rb",
  # tries: 1
# }
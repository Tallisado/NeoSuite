commander: nightly

# PLACEHOLDER
# fmfm_unittest_and_data_flow: { 
  # toolbox: webrobot, 
  # failover: die,
  # raketask: 'local:headless',
  # pattern: "ppm_fmfm_webrobot.rb",
  # tries: 1
# }

# phonedialog_setting_unittest_and_data_flow: { 
  # toolbox: webrobot, 
  # failover: die,
  # raketask: 'local:headless',
  # pattern: "ppm_settings_phonedialog_webrobot.rb",
  # tries: 1
# }

# spre_setting_unittest_and_data_flow: { 
  # toolbox: webrobot, 
  # failover: die,
  # raketask: 'local:headless',
  # pattern: "ppm_settings_spre_webrobot.rb",
  # tries: 1
# }

base_settings_unittest_and_data_flow: { 
  toolbox: webrobot, 
  failover: die,
  raketask: 'local:headless',
  pattern: "ppm_settings_webrobot.rb",
  tries: 1
}